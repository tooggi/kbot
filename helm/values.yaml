# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  repository: ghcr.io/tooggi
  tag: "v1.0.6-abbccf3"
  arch: amd64
  os: linux
secret:
  name: kbot-secret
  tokenName: TELE_TOKEN
  tokenKey: token
